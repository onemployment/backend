## Application Configuration
# Bind host (use 0.0.0.0 in containers)
HOST=0.0.0.0
# HTTP port
PORT=3000

## Redis Configuration
# Full connection URL used by the app
REDIS_URL=redis://redis:6379

## PostgreSQL Configuration
# Database credentials (used by Docker container)
POSTGRES_USER=appuser
POSTGRES_PASSWORD=DevPass2024!
POSTGRES_DB=onemployment
# PostgreSQL connection string (used by Prisma and application)
POSTGRES_DB_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}

## Authentication Configuration
# Bcrypt salt rounds for password hashing
SALT_ROUNDS=12

## Environment
# One of: development, test, production
NODE_ENV=development
